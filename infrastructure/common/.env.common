# Shared AWS Resources Configuration
# This file contains AWS resources shared between staging and production

# AWS Region
AWS_REGION=us-east-1

# Cognito (run 'terraform output' in common/ to get these values)
AWS_COGNITO_USER_POOL_ID=<from_terraform_output>
AWS_COGNITO_WEB_CLIENT_ID=<from_terraform_output>
AWS_COGNITO_MOBILE_CLIENT_ID=<from_terraform_output>
AWS_COGNITO_REGION=us-east-1

# ECR Repository URLs (run 'terraform output ecr_repository_urls' to get these)
ECR_BFF_REPO=<from_terraform_output>
ECR_CATALOG_REPO=<from_terraform_output>
ECR_CLIENT_REPO=<from_terraform_output>
ECR_DELIVERY_REPO=<from_terraform_output>
ECR_INVENTORY_REPO=<from_terraform_output>
ECR_ORDER_REPO=<from_terraform_output>
ECR_SELLER_REPO=<from_terraform_output>

# SQS (to be created separately if needed)
# AWS_SQS_QUEUE_URL=

# S3 (to be created separately if needed)
# AWS_S3_BUCKET=
