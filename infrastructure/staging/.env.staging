# Staging Environment Configuration
# This file contains staging-specific configuration

# Environment
ENVIRONMENT=staging
LOG_LEVEL=DEBUG

# Application metadata
APP_NAME=MediSupply Staging
APP_VERSION=1.0.0-staging

# Database URLs are set in docker-compose.yml as environment variables
# (pointing to Docker PostgreSQL containers)

# Service URLs (Docker network)
# These are set in docker-compose.yml for inter-service communication
