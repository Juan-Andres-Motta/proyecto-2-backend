version: '3.8'

services:
  order:
    build: .
    ports:
      - "8005:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: poetry run uvicorn main:app --host 0.0.0.0 --port 8000 --reload
